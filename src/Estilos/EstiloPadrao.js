import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    elements:{
        paddingHorizontal: 15,
        marginVertical: 250,
        marginHorizontal: 15,
        borderRadius: 10,
        borderWidth: 2,
        borderColor: '#222',
        fontSize: 16,
        fontWeight: "bold"
    }
})